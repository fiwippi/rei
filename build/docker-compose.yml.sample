version: '2'

services:
  rei-server:
    build:
      context: ../
      dockerfile: build/Dockerfile
    container_name: rei
    restart: always
    ports:
      - 8001:8001
    volumes:
      - ~/to-share:/shared
    environment:
      - READONLY=true
      - SKIP_HIDDEN_FILES=false